<h1>Song List</h1>
<%= link_to "New song", new_song_path %>

<table id="song_list">
  <thead>
    <tr>
      <th>Title</th>
      <th>Artist</th>
    </tr>
  </thead>
  
  <tbody>
    <% @songs.each do |song| %>
        <tr>
          <td><%= link_to song.title, song_path(song) %></td>
          <td><%= song.artist_name %></td>
        </tr>
    <% end %>
  </tbody>
</table>
